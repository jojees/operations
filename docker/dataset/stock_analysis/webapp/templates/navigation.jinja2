<nav class="navigation-default">
  <div class="nav-wrapper">

    <div class="left-nav">
{#      <a href="{{ url_for('dashboard_bp.dashboard') }}">
        <img src="{{ url_for('static', filename='img/logo.png') }}" alt="logo"/>
      </a>
#}
        <a href="{{ url_for('dashboard_bp.dashboard') }}">Dashboard</a>
        <a href="{{ url_for('indicators_bp.indicators') }}">Indicators</a>
    </div>

    <div class="right-nav">
      {% if request.blueprint != 'admin_bp'%}
        <a href="{{ url_for('admin_bp.admin') }}">Admin</a>
      {# {% else %}
        <a href="{{ url_for('dashboard_bp.dashboard') }}">Dashboard</a> #}
      {% endif %}
    </div>

  </div>
</nav>