version: 1.0.{build}
branches:
  only:
  - master
skip_tags: true
image: Ubuntu
shallow_clone: true
build:
  verbosity: minimal
only_commits:
  files:
    - docker/
skip_commits:
  files:
    - docker/**/.dockerignore
services:
- docker
test_script:
- make docker